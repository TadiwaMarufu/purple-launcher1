package com.thepurpleweb.purplelauncher.canvas

/**
 * Phase A: CLOCK only. Battery/Media/Calendar/Quote/AppShortcut are
 * Phase B — adding a new type here plus a branch in
 * CanvasModuleFactory.createContent() is the whole extension point.
 */
enum class CanvasModuleType {
    CLOCK
}
